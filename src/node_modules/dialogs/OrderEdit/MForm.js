import { MForm } from 'modules/Form';
import * as keys from './constants';

export default class MFormImpl extends MForm {
  getDefaultProps() {
    return {
      [keys.kStatusKey]: '',
      [keys.kReceiptNumberKey]:'',
      [keys.kTrackingNumberKey]: '',
      [keys.kNoticeRecipientKey]: true,
    }
  }
}