import React from 'react';
import { DialogComponent } from 'modules/Form';
import { SingleButton } from 'components/DialogToolbar';
import Filter from './Filter';
import './Order.css';

const kSelectedKey = 'selected';

export default class Catalog extends DialogComponent {
  render() {
    return (
        <div className="Catalog">
          <Filter className="Catalog__filter"/>
          <SingleButton onSubmit={this.onSubmit} />
        </div>
    );
  }
}