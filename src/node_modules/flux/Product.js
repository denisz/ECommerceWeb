import { Store } from 'modules/Flux';
import Actions from './ProductActions';
import XHR, { Methods } from './XHR';

export class Product extends Store {
  constructor() {
    super();

    this.id = 'product';
    this.listenables = Actions;
  }

  /**
   * Загрузка продукт из сервера
   * @param id
   * @returns {Promise.<void>}
   */
  async onFetch(id) {
    const { responseData } = await XHR(Methods.GET, { path: `catalog/product/${id}` });
    this.setState(responseData);
  }
}

export default new Product();
