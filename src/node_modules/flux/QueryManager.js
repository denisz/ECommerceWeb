import Query from 'modules/Request/Query';

export default {
  /**
   * Список коллекции
   * @returns {Promise.<void>}
   */
  queryForCollections() {
    return Query.POST('/catalog/collections');
  },

  /**
   * Список продуктов
   * @param id
   */
  queryForProducts(id) {
    return Query.POST(`/catalog/products/${id}`);
  },

  /**
   * Список распродаж
   */
  queryForSales() {
    return Query.POST('/sales');
  },

  /**
   * Список заказов
   */
  queryForOrders() {
    return Query.POST('/orders/list');
  },

  /**
   * Брендовые товары
   * @param producer
   * @returns {*}
   */
  queryForProducer(producer) {
    const query = Query.POST('/catalog/search');
    query.appendPayload({ producer });
    return query;
  }
};

