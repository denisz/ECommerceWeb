import React, { Component } from 'react';
import Loader from 'react-loaders'
import 'loaders.css/loaders.css'

export default ({ type='line-scale-pulse-out-rapid', color = 'black', className, size }) => (
  <Loader type={type} color="black" innerClassName={className}/>
);
