import React from 'react';
import cx from 'classnames';
import './OwnerName.css';

export default ({ value, className, ...rest}) => (
    <div className={cx('OwnerName', className)} {...rest} >
      {value}
    </div>
)