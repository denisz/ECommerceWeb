import React, { Component } from 'react';
import Image from 'components/Image';
import Email from 'components/Email';
import Phone from 'components/Phone';
import { corpEmail, cortPhone } from '../../../config';
import './Footer.css';

export default class Footer extends Component {
  static socials = [
    { type: 'vk', url:'' },
    { type: 'fb', url:'' },
    { type: 'inst', url:'' },
    { type: 'telegram', url:'' },
  ];

  render() {
    return (
      <div className="Footer vcard">
        <Image src="logo.png" className="Footer-logo"/>
        <div className="Footer-sections">
          <div className="Footer-contacts">
            <div className="Footer-contacts-phone">
              <Phone className="tel" tel={cortPhone} style={{color: 'white'}} />
            </div>
            <div className="Footer-contacts-time workhours">
              <div>пн.-пт. с 10-00 до 22-00</div>
              <div>сб. с 11-00 до 17-00</div>
            </div>
            <div className="Footer-contacts-email">
              <Email style={{color: 'white'}} email={corpEmail}/>
            </div>
          </div>
          <div className="Footer-social">
            {
              Footer.socials.map((i, idx)=>
                <a key={idx} href={i.url} alt={i.type}>
                  <Image src={`ic_${i.type}.png`} className="Footer-social-item"/>
                </a>)
            }
          </div>
        </div>
      </div>
    );
  }
}

Footer.propTypes = {};
Footer.defaultProps = {};
