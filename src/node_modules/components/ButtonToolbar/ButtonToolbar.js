import React from 'react';
import cx from 'classnames';
import './ButtonToolbar.css';

export default ({ className, children, left = false, right = false, center = false }) => (
  <div className={cx(className, 'ButtonToolbar', {
    'ButtonToolbar_left': left,
    'ButtonToolbar_right': right,
    'ButtonToolbar_center': center,
  })}>{children}</div>
)
