import React from 'react';
import cx from 'classnames';
import './Weight.css';

export default ({ value, className, ...rest}) => (
    <div className={cx('Weight', className)} {...rest}>{`${value} г`}</div>
)