import React from 'react';
import cx from 'classnames';
import './Item.css';

export default ({ displayName, value, className }) => (
  <div className={cx(className, 'GeoSuggestItem')}>
    <div className="GeoSuggestItem-title">{displayName}</div>
    <div className="GeoSuggestItem-subtitle">{value}</div>
  </div>
)
