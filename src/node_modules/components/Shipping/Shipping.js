import React from 'react';
import cx from 'classnames';
import localize from 'services/localizedDelivery';
import './Shipping.css';

export default ({ provider, method, className }) => (
    <div className={cx('Shipping', className)}>{localize({ provider, method})}</div>
);