import React from 'react';
import cx from 'classnames';
import './Invoice.css';

export default ({ className, value, ...rest }) => (
    <div className={cx('Invoice', className)} {...rest}>{`№${value}`}</div>
)