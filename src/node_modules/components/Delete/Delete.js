import React from 'react';
import cx from 'classnames';
import './Delete.css';

export default ({ onClick, className, children }) => (
  <button onClick={onClick} type="button" className={cx('Delete', className)} />
)
