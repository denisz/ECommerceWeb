import React from 'react';
import cx from 'classnames';
import './Title.css';

export default ({ children, className, ...rest }) => (
  <div className={cx('Title', className)} {...rest}>{children}</div>
)
