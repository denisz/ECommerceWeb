import React from 'react';

export default ({ className, onClick, src }) => (
  <div style={{
    backgroundImage: `url(${src})`,
    backgroundSize: 'auto 100%',
    backgroundRepeat: 'no-repeat',
    backgroundPosition: '50% 50%'
  }} className={className} onClick={onClick}/>
)
