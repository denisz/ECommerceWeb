import React from 'react';
import cx from 'classnames';
import Button from 'components/Button';
import ButtonToolbar from 'components/ButtonToolbar';
import './DialogToolbar.css';

export default ({onSubmit, onCancel, labelCancel = 'Закрыть', labelSubmit = 'OK', color = 'dark', className}) => (
    <ButtonToolbar className={cx('DialogToolbar', className)}>
      <Button
          className="DialogToolbar__btn"
          color={color}
          onClick={onCancel}>{labelCancel}</Button>
      <Button
          className="DialogToolbar__btn"
          color={color}
          onClick={onSubmit}>{labelSubmit}</Button>
    </ButtonToolbar>
)

const SingleButton = ({onCancel, labelCancel = 'OK', color = 'dark', className}) => (
    <ButtonToolbar className={cx('DialogToolbar', className)}>
      <Button
          className="DialogToolbar__btn"
          color={color}
          onClick={onCancel}>{labelCancel}</Button>
    </ButtonToolbar>
);

export { SingleButton }