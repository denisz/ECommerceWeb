import React from 'react';
import cx from 'classnames';
import Button from 'components/Button';
import ButtonToolbar from 'components/ButtonToolbar';
import './DialogToolbar.css';

export default ({onSubmit, onCancel, labelCancel = 'Закрыть', labelOK = 'OK', style = 'dark', className}) => (
    <ButtonToolbar className={cx('DialogToolbar', className)}>
      <Button
          className="DialogToolbar__btn"
          style={style}
          onClick={onCancel}>{labelCancel}</Button>
      <Button
          className="DialogToolbar__btn"
          style={style}
          onClick={onSubmit}>{labelOK}</Button>
    </ButtonToolbar>
)

const SingleButton = ({onCancel, labelOK = 'OK', style = 'dark', className}) => (
    <ButtonToolbar className={cx('DialogToolbar', className)}>
      <Button
          className="DialogToolbar__btn"
          style={style}
          onClick={onCancel}>{labelOK}</Button>
    </ButtonToolbar>
);

export { SingleButton }