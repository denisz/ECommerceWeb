import React from 'react';
import './OrderStatus.css';
import cx from 'classnames';
import localize from 'services/localizedOrderStatus';

export default ({ value, className}) => (
  <div className={cx('OrderStatus', className)}>{localize(value)}</div>
)