import React from 'react';
import cx from 'classnames';
import './Button.css';

export default ({ onClick, className, children }) => (
  <div onClick={onClick} className={cx('Button', className)}>{children}</div>
)
