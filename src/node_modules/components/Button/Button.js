import React from 'react';
import cx from 'classnames';
import './Button.css';

export default ({ onClick, className, children }) => (
  <button onClick={onClick} type="button" className={cx('Button', className)}>{children}</button>
)
