import React from 'react';
import cx from 'classnames';
import './Button.css';

const noop = () => {
};
export default ({onClick, disabled, className, children}) => (
    <div onClick={disabled ? noop : onClick} disabled={disabled}
         className={cx('Button', className, {
           'disabled': disabled,
         })}>{children}</div>
)
