import React from 'react';
import cx from 'classnames';
import './Sold.css';

export default ({disabled}) => (
    <div className={cx('Sold', {
      'disabled': disabled,
    })}>Нет в наличии</div>
)