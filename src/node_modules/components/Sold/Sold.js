import React from 'react';
import cx from 'classnames';
import './Sold.css';

export default ({disabled, className}) => (
    <div className={cx('Sold', className, {
      'disabled': disabled,
    })}>Нет в наличии</div>
)