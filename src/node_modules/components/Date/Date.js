import React from 'react';
import fecha from 'fecha';
import './Date.css';

export default ({ format='DD-MM-YYYY HH:mm', value }) => (
    <span className="Date">{fecha.format(new Date(value), format)}</span>
)