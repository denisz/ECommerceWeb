import React from 'react';
import fecha from 'fecha';
import './Date.css';

export default ({ format='DD-MM-YYYY HH:mm', value }) => (
    <div className="Date">{fecha.format(new Date(value), format)}</div>
)