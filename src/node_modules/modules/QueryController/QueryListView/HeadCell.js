import React, { Component } from 'react';
import cx from 'classnames';

export default class HeadCell extends Component {
  render() {
    const {style, text, onClick, className} = this.props;
    return (
        <li style={style}
            onClick={onClick}
            className={cx(className)}>
          {text}
        </li>
    );
  }
}