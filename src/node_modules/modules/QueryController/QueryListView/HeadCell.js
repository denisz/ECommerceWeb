import React from 'react';
import cx from 'classnames';

export default ({style, onClick, children, className}) => (
    <li style={style}
        onClick={onClick}
        className={cx(className)}>
      {children}
    </li>
)