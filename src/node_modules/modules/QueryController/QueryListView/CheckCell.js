import React from 'react';
import Checkbox from 'components/Checkbox';

export default ({className, checked, onChange}) => (
    <div className={className}>
      <Checkbox checked={checked}
                onChange={onChange}/>
    </div>
)