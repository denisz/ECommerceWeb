import { isObject } from 'underscore';
import MForm from './MForm';

export default (object) => {
  if (isObject(object)) {
    if (object instanceof MForm) {
      return object;
    }
    return MForm.assign(object);
  }

  return MForm.empty();
};
